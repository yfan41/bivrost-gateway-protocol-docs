---
title: "Bivrost Gateway Protocol"
description: "HTTP / MODBUS / MQTT / database / MCP protocol reference for the Bivrost IIoT gateway, intended for developers integrating with the gateway programmatically."
sidebar:
  label: "Introduction"
---

import { CardGrid, LinkCard, Steps, Aside } from '@astrojs/starlight/components';

export const base = import.meta.env.BASE_URL.replace(/\/+$/, '');

This document is the communication protocol reference for the Bivrost gateway, version v1.19.7, intended for developers who need to integrate with the gateway programmatically. For instructions on installing and configuring the gateway and using its web management interface, please refer to the [Bivrost Gateway Manual](https://docs.bivrost.cn/gateway/) (referred to below as the "Manual").

## Communication Methods

<CardGrid>
  <LinkCard
    title="HTTP Communication"
    href={`${base}/en/http/`}
    description="Actively read/write machine data, transfer files, analyze data, and configure the gateway via HTTP interfaces. All features have corresponding interfaces."
  />
  <LinkCard
    title="MODBUS Communication"
    href={`${base}/en/modbus/`}
    description="The gateway acts as a MODBUS server (port 502), storing automatic data collection task results in the corresponding address locations."
  />
  <LinkCard
    title="MQTT Communication"
    href={`${base}/en/mqtt/upload-format/`}
    description="The gateway publishes automatic data collection task results as JSON messages to the specified MQTT server, and supports an RPC interface."
  />
  <LinkCard
    title="Database Communication"
    href={`${base}/en/database/`}
    description="The gateway writes automatic data collection task results to the specified database (InfluxDB, MySQL, SQL Server, PostgreSQL, etc.)."
  />
  <LinkCard
    title="MCP Service"
    href={`${base}/en/mcp/`}
    description="The gateway's built-in MCP server exposes machine data, data analysis, and gateway status to AI clients as read-only tools."
  />
</CardGrid>

<Aside type="note" title="Note">
  MODBUS, MQTT, and database communication all output the results of **automatic data collection tasks**. Before use, you need to enable the automatic data collection task for the target machine/group on the gateway management page (see the Manual, [5.3.1.2. Task Settings](https://docs.bivrost.cn/gateway/usage/machines#task-settings) and [5.4.1.2. Group Task Settings](https://docs.bivrost.cn/gateway/usage/groups#group-tasks)) and restart the service.
</Aside>

## Reading Guide

<Steps>

1. Start with [1. Important Notes](/en/conventions/identifiers/) for the common identifiers, data classes, and variable definitions.

2. Then consult the section for the communication method you use.

3. If you don't have an actual machine tool available, use the [mock machine](/en/mock-testing/) to quickly test the communication protocol.

</Steps>

## More Resources

<CardGrid>
  <LinkCard
    title="FAQ"
    href={`${base}/en/faq/`}
    description="Common errors and troubleshooting steps for each communication method."
  />
  <LinkCard
    title="Version Change History"
    href={`${base}/en/changelog/`}
    description="Protocol and documentation changes in each gateway version."
  />
  <LinkCard
    title="Bivrost Gateway Manual"
    href="https://docs.bivrost.cn/gateway/"
    description="Instructions for installing and configuring the gateway and using its web management interface."
  />
</CardGrid>
